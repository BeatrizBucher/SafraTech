const model = require('../models/model/')
const moment = require('../moment')

const HistoricosController = {
    listaHitoricoCompleto: async (req,res) =>{
        try{
            //Lista historico geral, join entre evento_Ativadores e evento_Sensores
            
        }
        catch(erro){

        }
    },
    listaHitorico: async (req,res) => {
        try{
            //lista historico por id, baseado no id da area, join entre evento_Ativadores e evento_Sensores
        }
        catch(erro){
            
        }
    }
}

module.exports = HistoricosController